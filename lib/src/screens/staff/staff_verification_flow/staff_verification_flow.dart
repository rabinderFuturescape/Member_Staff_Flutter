import 'package:flutter/material.dart';
import 'mobile_verification_screen.dart';

/// Main entry point for the staff verification flow.
class StaffVerificationFlow extends StatelessWidget {
  const StaffVerificationFlow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const MobileVerificationScreen();
  }
}
